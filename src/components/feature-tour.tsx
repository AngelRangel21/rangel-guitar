// Este componente ha sido deshabilitado temporalmente debido a un problema
// de compatibilidad en tiempo de compilación entre la biblioteca 'react-joyride' y
// la versión actual de React. Al eliminarlo, se asegura que la aplicación
// pueda compilarse correctamente.
// En el futuro, podría reactivarse si la biblioteca se actualiza o si se
// encuentra una solución alternativa para el tour de características.

/**
 * Componente (actualmente deshabilitado) para un tour guiado de las características de la aplicación.
 * @returns {null} - Actualmente no renderiza nada.
 */
export function FeatureTour() {
    return null;
}
