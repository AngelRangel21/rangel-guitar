// This component has been temporarily disabled due to a build-time
// compatibility issue between the 'react-joyride' library and the current
// React version. Removing it ensures the application can build successfully.
export function FeatureTour() {
    return null;
}
